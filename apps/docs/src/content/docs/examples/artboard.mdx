---
title: Artboard quick start
description: Create, move, resize, and snap artboards with km-artboard.
---

import { LiveExample } from '../../../components/LiveExample';

`@komeilm76/km-artboard` provides pure geometry functions for rectangular artboards. Every function is immutable — it returns new data rather than mutating inputs.

## Live example

Edit the code below and the output panel updates instantly.

<LiveExample
  client:only="react"
  title="Artboard quick start"
  code={`import { artboard } from '@komeilm76/km-geoboard';

// ── 1. Create an artboard ─────────────────────────────────────────────────
const board = artboard.createArtboard({
  startPoint: { x: 0, y: 0 },
  endPoint: { x: 800, y: 600 },
  name: 'Main canvas',
});
console.log('Created:', board.name, board.width + 'x' + board.height);

// ── 2. Move it ────────────────────────────────────────────────────────────
const moved = artboard.moveArtboard(board, { x: 50, y: 50 });
console.log('Moved to:', moved.startPoint.x, moved.startPoint.y);

// ── 3. Resize it ──────────────────────────────────────────────────────────
const resized = artboard.resizeArtboard(moved, 1200, 900);
console.log('Resized to:', resized.width + 'x' + resized.height);

// ── 4. Snap to grid ───────────────────────────────────────────────────────
const snapped = artboard.snapArtboardToGrid(resized, 100);
console.log('Snapped start:', snapped.startPoint.x, snapped.startPoint.y);

// ── 5. Point containment ──────────────────────────────────────────────────
const inside = artboard.artboardContainsPoint(snapped, { x: 400, y: 300 });
const outside = artboard.artboardContainsPoint(snapped, { x: 5, y: 5 });
console.log('Point (400,300) inside:', inside);
console.log('Point (5,5) inside:', outside);

// ── 6. Overlap detection ─────────────────────────────────────────────────
const other = artboard.createArtboard({
  startPoint: { x: 700, y: 500 },
  endPoint: { x: 1400, y: 1000 },
  name: 'Overlay',
});
const overlap = artboard.artboardsOverlap(snapped, other);
console.log('Boards overlap:', overlap);
`}
/>

## Key concepts

**Normalization** — `createArtboard` always produces a canonical artboard: `origin` is the top-left corner, `size` is always positive. You can pass drag points in any order.

**Discriminated results** — fallible functions return `{ success: true, artboard }` or `{ success: false, reason }`. Infallible functions like `moveArtboard` return `Artboard` directly.

**Immutability** — no function mutates its input. Each call returns a new object.

## API summary

| Function | Returns | Notes |
|---|---|---|
| `createArtboard(input)` | `CreateArtboardResult` | Normalizes from two drag points |
| `resizeArtboard(input)` | `CreateArtboardResult` | Partial overrides for origin/size |
| `moveArtboard(input)` | `Artboard` | Translates by delta; infallible |
| `snapArtboardToGrid(input)` | `Artboard` | floor / round / ceil modes |
| `artboardContainsPoint(input)` | `boolean` | Inclusive bounds |
| `artboardsOverlap(input)` | `boolean` | True if any overlap exists |
| `artboardToRect / artboardFromRect` | `Rect / Artboard` | Convert to/from DOMRect-style |
