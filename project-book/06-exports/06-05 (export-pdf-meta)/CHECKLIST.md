# Checklist — Page 06-05 Export to PDF Meta

- [ ] `exportToPdfMeta.ts` created and exported from `index.ts`
- [ ] `PAGE_SIZES` constant defined with A4, A3, Letter, Legal in points
- [ ] Default `pageSize` is `"A4"`
- [ ] Default `orientation` is `"portrait"`
- [ ] A4 portrait: width ≈ 595.28, height ≈ 841.89
- [ ] Landscape orientation swaps width and height
- [ ] Custom `{ width, height }` page size passed through unchanged
- [ ] `svgContent` generated by calling `exportToSvg` with `xmlDeclaration: false`
- [ ] Errors from `exportToSvg` propagated as `Result` failures
- [ ] `title` and `author` are optional — omitted from output when not provided
- [ ] `artboard` field in output is the same input artboard
- [ ] Tests: A4 portrait, A4 landscape, custom page size, svgContent present
- [ ] Tests: title/author preserved, exportToSvg error propagated
- [ ] All tests pass
- [ ] `pnpm --filter @komeilm76/km-exports build` succeeds
- [ ] `grep -rn "^import.*zod" packages/km-exports/dist/` returns empty
