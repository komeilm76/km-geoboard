# Checklist — Page 06-05 Export to PDF Meta

- [x] `exportToPdfMeta.ts` created and exported from `index.ts`
- [x] `PAGE_SIZES` constant defined with A4, A3, Letter, Legal in points
- [x] Default `pageSize` is `"A4"`
- [x] Default `orientation` is `"portrait"`
- [x] A4 portrait: width ≈ 595.28, height ≈ 841.89
- [x] Landscape orientation swaps width and height
- [x] Custom `{ width, height }` page size passed through unchanged
- [x] `svgContent` generated by calling `exportToSvg` with `xmlDeclaration: false`
- [x] Errors from `exportToSvg` propagated as `Result` failures
- [x] `title` and `author` are optional — omitted from output when not provided
- [x] `artboard` field in output is the same input artboard
- [x] Tests: A4 portrait, A4 landscape, custom page size, svgContent present
- [x] Tests: title/author preserved, exportToSvg error propagated
- [x] All tests pass
- [x] `pnpm --filter @komeilm76/km-exports build` succeeds
- [x] `grep -rn "^import.*zod" packages/km-exports/dist/` returns empty
